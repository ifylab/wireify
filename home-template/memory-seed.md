# Wireify memory — {{GH_FILE}}

Per-definition lessons for this Grasshopper file. As you work, append what failed and what fixed it (symptom -> cause -> fix), plus any conventions specific to this definition. The `wireify-loop` skill drives this; the shared `~/.ify/wireify/defaults.md` holds the cross-file standards.

## Lessons

<!-- newest first; symptom -> cause -> fix -->
